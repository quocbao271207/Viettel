# === macOS / OS ===
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# === Virtual environments (KHÔNG push — dùng requirements.txt để tái tạo) ===
venv/
.venv/
env/
ENV/

# === Secrets / credentials (TUYỆT ĐỐI không push) ===
.env
.env.*
!.env.example
*.key
*.pem
*credentials*.json
*secret*.json
serviceAccount*.json

# === Editors / misc ===
.idea/
.vscode/
*.log

# === Output: giữ .zip có ý nghĩa, bỏ thư mục giải nén trung gian ===
out/*/output/
out/candidates/*/
# === Index/model tái tạo được, quá lớn cho git ===
data/icd10_sapbert.pt
# === Phiếu LLM: GIỮ trong git (claude.json không tái tạo được, nền điểm 30.26).
# Chỉ JSON thuần {text,type,assertion,before} — không chứa key. ===
